# Base de datos (Railway provee DATABASE_URL automáticamente)
DATABASE_URL="postgresql://user:password@host:5432/inyecta_stpb"

# JWT — cadena larga y aleatoria (mínimo 32 caracteres)
JWT_SECRET="cambia-esto-por-algo-muy-secreto-en-produccion"

# Entorno
NODE_ENV="production"

# Puerto (Railway lo asigna automáticamente, no cambiar)
PORT=3001
